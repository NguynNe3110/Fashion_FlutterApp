targets:
    $default:
        sources:
            include:
                - $package$
                - pubspec.yaml
                - lib/**
            exclude:
                - lib/src/main.dart
