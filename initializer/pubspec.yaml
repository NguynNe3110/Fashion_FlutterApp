name: initializer
description: A starting point for Dart libraries or applications.
version: 1.0.0
resolution: workspace

environment:
    sdk: ">=3.9.0 <4.0.0"

dependencies:
    shared:
    domain:
    data:
